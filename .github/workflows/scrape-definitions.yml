name: Generate Definitions
on:
  schedule:
    - cron:  '0 */2 * * *' # once every other hour

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - run: npm ci
    - run: npm run scrape-definitions
